# Firebase Admin SDK service account for local scripts only.
# Download from Firebase Console → Project settings → Service accounts → Generate new private key.
# Rename it to service-account.json and place it in the repo root (it is gitignored).
GOOGLE_APPLICATION_CREDENTIALS=service-account.json

# Firebase project ID (used by admin scripts as a fallback).
FIREBASE_PROJECT_ID=
